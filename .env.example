# Staging: https://livsighttest.didierdjakoua.site
# Local:  http://localhost:4040 (api-gateway — routes /api/* and /auth/*)
EXPO_PUBLIC_GATEWAY_URL=

# Push (optional in dev — disabled by default to avoid Expo token network errors)
# EXPO_PUBLIC_ENABLE_PUSH=true
